export class RegisterWorksheet {
  constructor() {
  }

  register(customerId: string, accountId: string): void {
    // comunicacion con los puertos para traer informacion del cliente
    // obtener la cuenta
    // obtener el contact del customer
    // obtener el contacto del cliente al momento de crear al customer
  }
}
